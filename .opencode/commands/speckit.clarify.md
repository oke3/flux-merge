# Speckit: Clarify

Resolves ambiguous or incomplete requirements in a specification.

## Usage
Invoke via the `speckit.clarify` command in opencode.json.

## Workflow
1. Load the specification from `.specify/specs/`
2. Identify ambiguous, contradictory, or incomplete sections
3. Generate clarifying questions or alternative interpretations
4. Update the specification with resolved clarifications
